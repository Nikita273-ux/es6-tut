//var sum = 10;.log(sum);
const doSomething = (action, data, method) => {

    console.log('performing ${action}...');
    method(data);
}

doSomething = ('multiply',[10,50], multiply);
doSomething = ('division',[10,2], division);
doSomething = ('addition',[10,20], addition);
doSomething = ('subtraction',[30,10], subtraction);

switch (key) {
    case value:
        
        break;

    default:
        break;
}